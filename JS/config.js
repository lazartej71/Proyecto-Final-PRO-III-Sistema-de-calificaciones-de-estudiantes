// Configuración global de la aplicación.
// URL base de la API que expone json-server. Si cambiás el puerto del backend,
// este es el único lugar que tenés que tocar.
export const API_URL = 'http://localhost:3000';
